what is SQL?
___________________________________________________
SQL structure Query language?
What is SQL?
SQL is used for perform the operation on RDBMS
SQL lets you access and manipulate databases
SQL became a standard of the American National Standards Institute (ANSI) in 1986, 
and of the International Organization for Standardization (ISO) in 1987
like as save record, view record, delete etc
____________________________________________________
What Can SQL do?

SQL can execute queries against a database
SQL can retrieve data from a database
SQL can insert records in a database
SQL can update records in a database
SQL can delete records from a database
SQL can create new databases
SQL can create new tables in a database
SQL can create stored procedures in a database
SQL can create views in a database
SQL can set permissions on tables, procedures, and views
________________________________________________


Q. whats is RDMBS?

RDMS stand for Relational Database Management System

RDBMS means database strucure where we can store the data in tubular format or in the
form of row and column.

RDBMS is the basis for SQL, and for all modern database systems such as MS SQL Server,
IBM DB2, Oracle, MySQL, and Microsoft Access.

The data in RDBMS is stored in database objects called tables.
A table is a collection of related data entries and it consists of columns and rows.
____________________________________________________

Q. what is database ? 
database is collection of one or more than one tables where we can store the data permentatle for future use
_____________________________________________________
Q. where we create databas and store data perform operation on it?
or which tools or software i can use for creating database and work?

if we want to work with RDBMS and SQL we have the n number of toolls or saftware
like as MySQL, SQL Server, MS Access, Oracle, Sybase, Informix, Postgres.

_________________________________

Some of The Most Important SQL Commands.
SELECT - extracts data from a database
UPDATE - updates data in a database
DELETE - deletes data from a database
INSERT INTO - inserts new data into a database
CREATE DATABASE - creates a new database
ALTER DATABASE - modifies a database
CREATE TABLE - creates a new table
ALTER TABLE - modifies a table
DROP TABLE - deletes a table
CREATE INDEX - creates an index (search key)
DROP INDEX - deletes an index


